<!-- Generated by `llm-browser docs --write`; edit the models, not this file. -->

# Extract specs

What a `read` step's `extract:` writes, one entry per column.

## The compact form

Read the compact `"child selector@attribute"` form.

Every half is optional: `"td.name"` reads that child's text,
`"@href"` reads the attribute off the row element itself,
`"td.name@href"` does both, and `""` is the row's own text.

## The mapping form

A model field marked for HTML extraction.

Use as a default value, like Pydantic's `Field()`. `child_selector`
descends into a child of the matched row; if `None`, the value is
read off the row element itself. `attribute` is what to read —
one of `constants.EXTRACT_PROPERTIES` (`textContent` by default)
or any HTML attribute name.

## Readable properties

Every name below is read as a JavaScript property; anything else is an HTML attribute. The default is `textContent`, under the field name `text` when the spec names no fields.

- `childElementCount`
- `innerHTML`
- `innerText`
- `outerHTML`
- `tagName`
- `textContent`
- `value`

A `read` step's `exclude:` only changes the properties a descendant is part of: `childElementCount`, `innerHTML`, `innerText`, `outerHTML`, `textContent`.
