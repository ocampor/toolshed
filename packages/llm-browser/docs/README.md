# llm-browser docs

The docs ship inside the wheel, at [`src/llm_browser/docs/`](../src/llm_browser/docs/):
`guide/` is hand-written, `reference/` is generated from the models by
`llm-browser docs --write`. Read them from Python with `llm_browser.docs`.
`RESEARCH.md` beside this file is history, not documentation.
