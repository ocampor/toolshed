# Two screenshots: one with a `path:` the runner must ignore, one without.
params:
  - path
steps:
  - name: explicit
    action: screenshot
    path: "{{ path }}"
  - name: session
    action: screenshot
