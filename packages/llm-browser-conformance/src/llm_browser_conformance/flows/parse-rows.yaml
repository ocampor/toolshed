# `parse` coerces each row to the schema; `path:` is for the CLI and the
# runner has to ignore it.
steps:
  - name: rows
    action: parse
    selector: "tr.row"
    schema_path: "{{ schema }}"
    path: "{{ out }}"
