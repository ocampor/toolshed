# The same wait against `aria-disabled`, which a div-and-role control uses
# because it cannot carry the `disabled` attribute.
steps:
  - { name: agree, action: check, selector: "#agree" }
  - name: unlocked
    action: wait_for
    selector: "#submit"
    state: enabled
    timeout: 8000
    interval: 200
